/* Carrito: layout dos columnas con resumen a la derecha */
.cart-page {
    padding: 2rem 1rem;
}

.cart-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
}

@media (min-width: 980px) {
    .cart-grid {
        grid-template-columns: 2fr 1fr;
        align-items: start;
    }
}

.cart-left-head {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 1rem;
    padding: .5rem 0;
    color: #555;
    font-weight: 700;
    text-transform: uppercase;
    font-size: .9rem;
}

.cart-left {
    background: #fff;
    border-radius: 12px;
    padding: 1rem;
    box-shadow: 0 1px 10px rgba(0, 0, 0, .06);
}

.cart-list {
    display: grid;
    gap: 1rem;
}

.cart-empty {
    color: #666;
    margin: 1rem 0;
}

.cart-item {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 1rem;
    border-top: 1px solid #eee;
    padding-top: 1rem;
}

.cart-item:first-child {
    border-top: 0;
    padding-top: 0;
}

.ci-main {
    display: grid;
    grid-template-columns: 80px 1fr;
    gap: 1rem;
    align-items: start;
}

.ci-figure {
    width: 80px;
    height: 80px;
    border: 1px solid #eee;
    border-radius: 8px;
    overflow: hidden;
    background: #fafafa;
    display: flex;
    align-items: center;
    justify-content: center;
}

.ci-figure img {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.ci-info {
    display: grid;
    gap: .35rem;
}

.ci-title {
    font-size: 1rem;
    margin: 0;
}

.ci-price {
    color: #666;
    margin: 0;
}

.ci-qty {
    display: inline-flex;
    align-items: center;
    gap: .5rem;
    margin-top: .25rem;
}

.ci-btn {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    border: 1px solid #ddd;
    background: #f7f7f7;
    cursor: pointer;
}

.ci-count {
    min-width: 1.5rem;
    text-align: center;
    font-weight: 600;
}

.ci-remove {
    background: none;
    border: none;
    color: #666;
    text-decoration: underline;
    cursor: pointer;
    padding: 0;
    font-size: .9rem;
}

.ci-total {
    font-weight: 700;
}

.cart-clear {
    margin-top: 1rem;
    background: none;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: .6rem .9rem;
    cursor: pointer;
}

.cart-right {
    background: #fff;
    border-radius: 12px;
    padding: 1rem;
    box-shadow: 0 1px 10px rgba(0, 0, 0, .06);
    position: sticky;
    top: calc(var(--nav-h, 64px) + 16px);
}

.cr-title {
    margin-top: 0;
    font-size: 1.1rem;
}

.cr-cupon {
    border-top: 1px solid #eee;
    padding-top: .75rem;
}

.cr-cupon-row {
    display: flex;
    gap: .5rem;
    margin-top: .5rem;
}

.cr-cupon-row input {
    flex: 1;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: .6rem .8rem;
}

.cr-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 1.1rem;
    margin-top: 1rem;
}

.cr-discount {
    color: #0a7d2c;
    font-size: .95rem;
}

.cr-pay {
    width: 100%;
    margin-top: 1rem;
    background: #ff8800;
    color: #fff;
    border: none;
    border-radius: 10px;
    padding: .9rem 1.1rem;
    font-weight: 700;
    cursor: pointer;
}

/* Integración visual con diagonales de páginas internas */
body.is-inner .cart-page {
    position: relative;
}
